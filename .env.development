# 项目标识（多项目同机共用一套本地 Redis(6401/6402)/MySQL(3316) 时的命名空间）：
#  - Redis：全部键带 `<projectId>_` 前缀（server keys.ts 统一拼接）
#  - MySQL：库名 `game_<projectId>`（db:bootstrap 自动建库）
# 本文件缺失或未设置时默认 gono。第二个项目只需改这一个值（服务端口另见
# apps/server/.env.development 的 PORT，需各项目错开）。
projectId=gono
