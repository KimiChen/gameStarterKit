# 原生 Lobby

-   实现固定服客户端的 WebSocket Lobby：鉴权、在线归属、字符串路由、请求回复与同步帧。
-   传输层复用本地 Action、事件与 ModSync 生命周期；身份必须来自可信鉴权映射，不能以默认身份执行玩家写操作。
-   验证分组并发必须覆盖同一连接的真实请求入口，同时检查收包队列和身份队列是否等待整次业务完成；仅测试 Action 分组队列不能证明入口没有阻塞不同分组。
