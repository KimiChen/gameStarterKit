import { ReqDefault, ResDefault } from '../../../protocol/S2S/default'
import { GameAction } from '../../GameAction'

export class ActionDefault extends GameAction {
    async doAction(req: ReqDefault, res: ResDefault) {
        return
    }
}
