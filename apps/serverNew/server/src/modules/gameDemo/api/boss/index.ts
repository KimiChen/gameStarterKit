/** Boss operations route through GameDemoBossRpc so the host can select the room's task worker. */
export {
    GameDemoBossRpc,
    GameDemoBossPush,
} from '../../../../../generated/lobby-contract/native/lobbyRpc/domains/gameDemoBoss'
