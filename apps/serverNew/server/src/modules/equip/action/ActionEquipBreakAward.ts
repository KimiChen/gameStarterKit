import { ReqEquipBreakAward, ResEquipBreakAward } from '../EquipC2S'
import { GameAction } from '../../../runtime/action/GameAction'

/**
 * 突破奖励
 */
export class ActionEquipBreakAward extends GameAction {
    async doAction(req: ReqEquipBreakAward, res: ResEquipBreakAward) {
        return
    }
}
