import { ReqRankWorship, ResRankWorship } from '../RankC2S'
import { GameAction } from '../../../runtime/action/GameAction'

/**
 * 膜拜
 */
export class ActionRankWorship extends GameAction {
    async doAction(req: ReqRankWorship, res: ResRankWorship) {
        return
    }
}
