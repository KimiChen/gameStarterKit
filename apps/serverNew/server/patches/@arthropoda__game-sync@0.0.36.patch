diff --git a/lib/common.js b/lib/common.js
index 84be2a2cd26c8d4ed7b1975dff18fb0ea232ace0..5dc12463707e36fed5e18549e558bfea2771990b 100644
--- a/lib/common.js
+++ b/lib/common.js
@@ -34,6 +34,13 @@ function loadCfg() {
     let cfgPath = path.join(exports.workPath, 'tools-cfg.json');
     let jsonContent = fs.readFileSync(cfgPath, 'utf-8');
     const cfg = json5.parse(jsonContent);
+    const environmentPassword = process.env.GAME_SYNC_PASSWORD?.trim();
+    if (environmentPassword) {
+        cfg.pwd = environmentPassword;
+    }
+    if (!cfg.pwd) {
+        throw new Error('GAME_SYNC_PASSWORD is required when tools-cfg.json does not contain pwd');
+    }
     cfg.cfgExtPath = '.' + cfg.cfgExt;
     return cfg;
 }
diff --git a/src/common.ts b/src/common.ts
index 0dc554d076ffa5fec80c49e5066334c83f298393..20c6fc336f426b3edbb0d5f97a76ef9a7ffd6a1d 100644
--- a/src/common.ts
+++ b/src/common.ts
@@ -34,6 +34,13 @@ function loadCfg() {
     let cfgPath = path.join(workPath, 'tools-cfg.json');
     let jsonContent = fs.readFileSync(cfgPath, 'utf-8')
     const cfg: IConfig = json5.parse(jsonContent);
+    const environmentPassword = process.env.GAME_SYNC_PASSWORD?.trim()
+    if (environmentPassword) {
+        cfg.pwd = environmentPassword
+    }
+    if (!cfg.pwd) {
+        throw new Error('GAME_SYNC_PASSWORD is required when tools-cfg.json does not contain pwd')
+    }
     cfg.cfgExtPath = '.' + cfg.cfgExt
     return cfg
 }
