# 邮件 · 士兵详情

`MailSoldierDetails` / `MailSoldierDetailsPanel`，750×1624，预览路由 `mail-soldier-details`，界面名与标题「士兵详情」。布局真源为 `AI标准化/邮件/邮件-士兵详情_切图/reference/screen.png` 与切图参数。

- 窗体 (19,221) 714×1186，关闭 (680,223) 50×50；内容底 (40,303) 673×928，固定表头同位置 673×49。
- 表头与数值列对齐：阵亡、重伤、轻伤、击杀、剩余。重伤红字，其他数值深紫字；我方玩家名绿色，敌方红色。
- 单层变高 `VirtualList` 位于 (50,365) 656×851；玩家头 62px，士兵行 83px，展开数据展平到同一列表。列表底原图为 638×186 的纯色矩形，逐行适配为 83px；不嵌套虚拟列表。
- 原稿三组玩家，前两组展开、第三组收起；同名玩家使用独立 ID。各组示例总计为 0 / 999 / 20 / 0 / 0；每组两条士兵示例：数量 5432、等级 1、战损 0 / 50 / 1 / 0 / 0。总计与明细按原稿分别展示，不从示例明细推算。
- 头像使用本次切图提供的完整 53×64 头像图（SHA256 与现有 `ui/mail-report-detail/soldier-icon` 相同），等级角标文字独立叠加。窗体、关闭、内容底、表头、行底、箭头均按字节复用；只新增士兵列表底。
- 玩家头透明点击遮罩与视觉分离，按压不缩放、位移或变暗；展开箭头同步翻转。没有士兵的分组禁用折叠并隐藏箭头。

`groups` 可注入玩家分组、组总计和士兵列表，要求组 ID 与组内士兵 ID 稳定唯一。`onAction` 发出 `expand:<groupId>` / `collapse:<groupId>`，`onClose` 关闭。重新打开恢复 `initiallyExpanded` 并回到顶部。当前为展示数据，未接服务器。

战报详情「士兵对比」信息按钮和双方头像均打开本面板，与战斗日志、部队详情互斥。关闭保留战报详情滚动位置，支持邮件 → 战报详情 → 士兵详情逐层返回。

预览登记覆盖目录、原稿 `PreviewHome`、Web/Cocos 预览映射与 `createMailSoldierDetailsPreview`。

验收：`build:uniflex-ui` → `sync:client` → `typecheck:uniflex-ui`；从首页打开、独立开合、按住无缩放、关闭重开、战报与邮件链路。
