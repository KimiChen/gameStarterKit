/** Paint retained raster surfaces once into the same low-resolution target as Cocos. */
export declare function paintSnapshotElement(context: CanvasRenderingContext2D, element: HTMLElement, origin: DOMRect, width: number, height: number): void;
