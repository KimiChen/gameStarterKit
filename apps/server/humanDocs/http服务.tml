# HTTP 服务与区服服务关系梳理

> 文档范围：`/Users/kk/26/eerie-server/api-lumen` 与 `/Users/kk/26/alloy/alloy-server`。
>
> 本文只描述功能、部署边界、数据归属和请求流转，不展开具体代码和路由清单。

## 一、总览

### 1. 核心结论

两套系统的 HTTP 都不是“每个区服一套完整业务 API”。它们都更接近一个共享的中心入口：

- 接收客户端启动请求、渠道回调和运营后台请求；
- 访问中心账号、区服目录、平台配置和运营数据；
- 根据玩家归属、请求中的区服选择或跨服配置，把操作落到一个或多个区服；
- 必要时再调用对应区服的游戏服务，让区服内存状态、在线玩家或实时服务执行动作。

区服服务则是数据面和实时执行面：

- 持有单个区服的实时连接、房间、在线状态和区服专属缓存；
- 处理玩家进入游戏后的实时协议和区服内业务；
- 接收中心 HTTP 转发来的单区动作；
- 只对自身 `sId` 的数据和在线状态负责。

### 2. 统一架构脑图

```mermaid
mindmap
  root((游戏 HTTP 体系))
    共享中心 HTTP
      玩家启动
        登录
        区服列表
        公告
        包与资源版本
        客户端配置
      外部回调
        支付
        问卷
        渠道通知
      运营管理
        GM
        配置热更
        玩家查询与修改
        邮件与活动
        封禁与维护
      诊断工具
        日志
        Redis
        协议检查
        监控
    中心数据层
      中心数据库
      中心 Redis
      区服目录
      跨服配置
      订单与运营记录
    区服服务集群
      s1
        实时连接
        区服数据库
        区服 Redis
        在线玩家
      s2
      s3
      更多区服
    跨区编排
      根据 UID 找归属区
      根据 sId 批量执行
      根据跨服组扩散
      队列或内部调用
```

### 3. 标签说明

| 标签 | 含义 |
|---|---|
| `[共享入口]` | HTTP 进程通常只部署一套或少量实例，面向全部区服 |
| `[单区落地]` | 最终只修改一个区服的数据或在线状态 |
| `[多区批量]` | 一个请求选择多个区服并逐个执行 |
| `[跨服]` | 使用跨服组、赛季或中心数据协调多个区服 |
| `[中心数据]` | 只读写中心库、中心 Redis 或平台级配置 |
| `[区服数据]` | 读写指定区服数据库、区服 Redis 或区服进程 |
| `[节点本地]` | 只作用于某个 HTTP/游戏节点的本地文件或在线表 |
| `[外部回调]` | 第三方渠道、支付或问卷系统主动调用 |
| `[管理面]` | GM、Adjust、SSO、配置和诊断工具，不是实时对局通信 |

## 二、HTTP 与区服服务的统一关系

### 1. 玩家启动流程

```mermaid
flowchart LR
  C[客户端启动] --> H[共享 HTTP]
  H --> A[渠道身份校验]
  A --> CD[中心账号数据]
  H --> SL[区服目录与状态]
  SL --> R[返回可进入区服及连接信息]
  R --> Z[客户端连接选定区服服务]
  Z --> ZD[区服实时业务与区服数据]
```

要点：

- 登录和选区属于中心入口，不能理解为某个单独区服的登录服务；
- HTTP 返回区服地址、开服状态、维护状态或推荐区服；
- 玩家真正进入游戏后，实时通信和区服内状态由目标区服服务承担；
- 区服服务不应反过来承担全局区服目录和中心账号管理。

### 2. GM/运营操作流程

```mermaid
flowchart TD
  O[运营后台或 GM 工具] --> H[共享 HTTP 管理面]
  H --> V[权限、签名、IP、幂等校验]
  V --> D{目标范围}
  D -->|中心| C[中心库或中心 Redis]
  D -->|单区| S1[指定区服数据]
  D -->|多区| S2[遍历多个区服]
  D -->|跨服| S3[中心跨服记录 + 相关区服]
  S1 --> I[必要时调用区服内部动作]
  S2 --> I
  S3 --> I
  I --> Z[区服服务执行在线/内存动作]
  Z --> ACK[结果、日志、审计或补偿]
```

要点：

- HTTP 是编排层，不等于所有操作都在 HTTP 进程本地完成；
- 修改数据库和修改在线内存是两件事，通常需要先落库，再通知区服服务刷新；
- 多区操作必须记录每个区服的成功、失败和重试状态，不能只返回一个总成功；
- 只操作中心数据的功能不需要访问某个区服，但涉及在线玩家时必须触达对应区服服务。

### 3. 外部回调流程

```mermaid
sequenceDiagram
  participant P as 第三方渠道
  participant H as 共享 HTTP
  participant C as 中心订单/账号数据
  participant Z as 目标区服服务
  participant D as 区服数据

  P->>H: 支付/问卷/渠道回调
  H->>H: 验签、参数校验、幂等判断
  H->>C: 查询订单、玩家和渠道信息
  C-->>H: 返回玩家归属或业务上下文
  H->>Z: 投递区服动作
  Z->>D: 写入区服奖励、邮件或玩家状态
  Z-->>H: 返回处理结果
  H-->>P: 返回渠道要求的确认结果
```

## 三、`api-lumen` 的 HTTP 服务

### 1. 服务定位

`api-lumen` 是一个以中心 API 为核心的综合服务，使用中心数据库、多个区服数据库、中心 Redis 和战报/统计类数据源。它把玩家入口、渠道回调、GM、Adjust、战报和监控放在同一套 HTTP 应用中。

整体判断：`[共享入口] [多区] [中心编排]`。

### 2. 功能分类

| 功能域 | 主要职责 | 区服关系 |
|---|---|---|
| 玩家登录与区服入口 | 渠道登录、中心账号初始化、返回可用区服、GM 强登 | `[共享入口] [中心数据]`，返回多区列表 |
| 登录前信息 | 公告、推荐区服、维护/开服状态 | `[共享入口]`，通常中心或多区汇总 |
| 包与资源管理 | 包版本、资源版本、跳转地址、CDN 信息 | `[共享入口] [平台/线路级]` |
| 客户端配置 | 配置清单、热更配置、按区服配置下载 | `[共享入口]`，配置可 `[单区落地]` 或全局复用 |
| 渠道与支付 | 下单、支付回调、内部充值、问卷回调 | `[外部回调] [共享入口]`，按玩家/订单落到目标区 |
| GM | 玩家、封禁、邮件、活动、礼包、道具、公告、充值、服务器状态等 | `[管理面] [共享入口]`，支持 `[单区落地] [多区批量] [跨服]` |
| 战报 | 战报列表、详情、统计、城市/场景筛选、导出 | `[共享入口]`，可按区服、赛季或跨服数据查询 |
| Adjust 运营工具 | 配置热更、参数编辑、玩家数据、活动、Redis、测试用例、日志和 AI 工具 | `[管理面]`，按选定区服或跨服配置执行 |
| 监控与诊断 | 区服列表、Redis 状态、WebSocket 地址、异常详情、运行配置 | `[共享入口]`，可汇总多区，也可定位节点 |

### 3. 数据和区服边界

```mermaid
flowchart TB
  H[api-lumen HTTP]
  H --> CDB[中心数据库]
  H --> CR[中心 Redis]
  H --> SDB1[区服 1 数据库]
  H --> SDB2[区服 2 数据库]
  H --> SR1[区服 1 Redis]
  H --> SR2[区服 2 Redis]
  H --> FR[战报/统计数据源]
  H --> QS[区服动作队列或内部服务]
```

区服归属规律：

- 账号、区服目录、运营账号、全局公告、跨服活动索引等，主要属于中心数据；
- 玩家角色、邮件、活动实例、区服状态和区服缓存，属于具体 `sId`；
- 同一个 GM 请求可以同时写中心记录和多个区服记录；
- 某些实时动作会先写区服数据，再通知区服进程刷新内存或在线玩家；
- 监控类接口通常遍历区服配置，因此调用方看到的是多区汇总结果。

### 4. `api-lumen` 的典型流转

#### 玩家登录

```text
客户端 → 中心 HTTP → 渠道身份校验 → 中心账号保存
       → 读取区服目录 → 返回区服列表和登录凭证
       → 客户端自行连接选定区服
```

#### GM 修改玩家

```text
GM 工具 → 中心 HTTP → 权限/签名/幂等
        → 根据 UID 或 sId 找目标区服
        → 修改中心或区服数据
        → 通过队列/内部动作通知区服服务
```

#### 跨服活动

```text
运营配置 → 中心 HTTP → 保存跨服活动记录
         → 展开区服组 → 写入多个区服活动记录
         → 通知相关区服刷新活动
```

### 5. 部署判断

`api-lumen` 的 HTTP 进程应被视为中心管理/入口服务，而不是 `s1`、`s2` 各自独立的业务 HTTP。

但是，HTTP 进程访问的数据并非全部中心化：它必须能根据配置访问各区服数据库、区服 Redis，或通过队列/内部服务访问区服进程。因此它是“共享入口 + 分区落地”的架构。

## 四、`alloy-server` 的 HTTP 服务

### 1. 服务定位

Alloy 把管理 HTTP 和区服游戏服务拆得更明确：

- `src/http/main.ts` 启动的是管理 HTTP 进程；
- `src/main.ts` 启动的是固定区服游戏服务，并通过 `--sid` 绑定一个区服；
- 管理 HTTP 根据区服目录和固定区服配置找到目标区服；
- 管理 HTTP 可以把动作转发到区服服务的内部动作入口。

整体判断：管理 HTTP 是 `[共享入口] [中心编排]`，游戏服务是 `[单区服务]`。

### 2. 功能分类

| 功能域 | 主要职责 | 区服关系 |
|---|---|---|
| Center 登录 | 玩家登录、GM 登录、区服列表、登录公告、包版本 | `[共享入口] [中心数据]`，返回多区连接信息 |
| 客户端配置 | 正式配置、测试配置、热更清单、配置文件下载 | `[共享入口]`，可按 `sId` 读取测试配置 |
| 支付与问卷 | 支付下单、支付回调、问卷回调 | `[外部回调] [共享入口]`，动作可投递到指定区服 |
| GM | 封禁、玩家、区服、活动、礼包、邮件、道具、Redis、语言等 | `[管理面] [共享入口]`，支持单区和多区操作 |
| Adjust 玩家工具 | 玩家查询、数据树、数据修改、改名、新建角色、时间调整 | `[管理面]`，按 UID 找所属区服并转发 |
| Adjust 配置工具 | 配置查看、上传、删除、热更和配置历史 | `[管理面] [多区批量]`，写入指定区服配置并广播刷新 |
| Adjust 测试工具 | 测试用例、环境变量、批量测试计划、用户组、导入导出 | `[共享入口]`，管理数据可跨区使用 |
| Adjust AI 工具 | Prompt、知识库、配置 schema、源码检索和 AI 策略 | `[管理面] [中心/线路级]` |
| Redis 与协议工具 | 协议文件、协议版本、中心/用户/区服 Redis 查询与受控命令 | `[共享入口]`，可查看多个 Redis 域 |
| SSO 与权限 | SSO 登录、会话、用户管理、管理员权限、只读/读写边界 | `[中心管理面]` |
| 诊断 | 错误日志、Swagger、管理控制台静态资源 | 共享入口，也可跳转或读取具体节点 |

### 3. Alloy 的进程拓扑

```mermaid
flowchart LR
  U[客户端/运营工具/渠道] --> M[Alloy 管理 HTTP]
  M --> C[中心数据库与中心 Redis]
  M --> CFG[区服目录与固定区服配置]
  M --> S1[区服服务 s1]
  M --> S2[区服服务 s2]
  M --> S3[区服服务 s3]
  U -. 选区后实时连接 .-> S1
  U -. 选区后实时连接 .-> S2
  U -. 选区后实时连接 .-> S3
  S1 --> D1[s1 数据库/Redis/在线状态]
  S2 --> D2[s2 数据库/Redis/在线状态]
  S3 --> D3[s3 数据库/Redis/在线状态]
```

### 4. Alloy 的典型流转

#### 登录和进入区服

```text
客户端 → 管理 HTTP 登录
       → 中心账号处理和区服目录查询
       → 返回 s1/s2/... 的连接信息
       → 客户端连接选定的固定区服服务
```

#### Adjust 修改玩家

```text
Adjust → 管理 HTTP → SSO/IP/权限校验
       → 根据 UID 计算所属 sId
       → 调用该 sId 的区服内部动作
       → 区服服务修改玩家内存/持久数据并返回结果
```

#### 配置热更

```text
运营上传配置 → 管理 HTTP 保存中心配置记录
             → 写入一个或多个 sId 的测试配置
             → 广播配置重新加载
             → 各区服服务按自身范围刷新
```

### 5. Alloy 的部署判断

应把 Alloy 拆成两个部署概念：

| 部署单元 | 范围 | 主要职责 |
|---|---|---|
| 管理 HTTP | 多区共享 | 登录、区服目录、GM、Adjust、支付、配置、诊断和跨区编排 |
| 区服游戏服务 | 一个进程绑定一个 `sId` | 实时连接、房间、在线玩家、区服业务和区服数据 |

因此，Alloy 的“HTTP 服务”本身不是单区服服务；单区服属性位于 `src/main.ts` 启动的游戏服务及其数据源。

## 五、两套系统的对照

| 对照项 | api-lumen | alloy-server |
|---|---|---|
| HTTP 总体定位 | 综合中心 API，入口、运营、回调、监控集中 | 管理 HTTP 与区服游戏服务分层更明确 |
| HTTP 是否多区共享 | 是 | 是，管理 HTTP 共享 |
| 游戏服务是否单区 | 通常按区服数据/服务拆分，HTTP 侧通过配置访问 | 明确按 `--sid` 启动单区游戏服务 |
| 登录和选区 | 中心 HTTP 返回多区列表 | 管理 HTTP 返回固定区服连接信息 |
| GM/运营 | 动态 GM + Adjust，跨区能力较强 | 动态 GM + 模块化 Adjust，按 UID/sId 转发更明显 |
| 区服数据访问 | HTTP 可直接访问中心库和多个区服库 | 管理 HTTP 使用中心数据，并通过固定区服端点触达区服服务 |
| 跨区活动 | 中心记录 + 多个区服记录 | 中心配置 + 多区广播/动作投递 |
| 实时业务边界 | 由区服服务承载，HTTP 负责入口和编排 | 由固定 `sId` 游戏服务承载，边界更清晰 |
| 主要风险 | 中心 HTTP 直接触达多类数据，权限和区服边界容易混在一起 | 管理 HTTP 与单区服务的动作一致性、回调落区仍需逐链路验证 |

## 六、需要特别关注的边界

### 1. “共享 HTTP”不等于“所有数据共享”

共享的是入口和编排能力，不是玩家数据本身。玩家角色、区服邮件、区服活动、在线状态和区服 Redis 仍然应按 `sId` 隔离。

### 2. “单区操作”不等于“单区 HTTP 部署”

一个请求只处理 `sId=3`，只说明业务落点是单区；它仍然可以由共享管理 HTTP 接收，再转发到 3 区服务。

### 3. 数据库写入和在线状态修改必须分开看

例如封禁、改名、发邮件、修改玩家属性：

- 数据库写入可能已经成功；
- 在线玩家内存可能还没有刷新；
- 区服服务不可达时，需要重试、补偿或下次登录收敛；
- 运营结果不能只根据中心库写入成功来判断“玩家已经生效”。

### 4. 跨区操作必须有逐区结果

活动、邮件、公告、配置、维护等批量操作不能只返回一个总状态。至少要能区分：

- 哪些区服成功；
- 哪些区服失败；
- 是否已经写中心记录；
- 是否已经通知区服服务；
- 是否可以安全重试。

### 5. Alloy 支付回调存在多区落地风险

Alloy 的支付入口是共享的，但当前支付回调处理里仍存在订单区服字段固定为单一区号的遗留实现迹象。正式多区部署前，应单独核对“订单归属、玩家归属、发货区服、回调幂等”是否全链路使用真实 `sId`，不能只看入口已经支持多区。

### 6. API Lumen 的配置/调整功能也要区分中心和区服

API Lumen 的 Adjust 路由很多，既有中心配置，也有按区服或跨服的操作。审计时应以底层数据源和动作投递目标为准，不能把所有 `/adjust` 能力都视作“中心配置”。

## 七、最终判断

```text
两套系统共同模式：

共享 HTTP 入口
  = 登录、区服目录、渠道回调、GM、Adjust、配置、诊断、跨区编排

区服服务
  = 单区实时连接、房间、在线玩家、区服数据、区服内业务执行

中心数据
  = 账号、区服目录、全局配置、订单索引、跨服索引、运营权限

区服数据
  = 角色、背包、邮件、活动实例、在线状态、区服 Redis 和区服实时内存
```

如果后续要把这两套系统的能力迁移到新的服务端框架，建议保留这个分层：

1. HTTP 只做稳定的入口和编排；
2. 区服服务只负责单区实时状态和单区业务执行；
3. 中心数据与区服数据显式分离；
4. 跨区操作通过明确的批量任务、队列或内部调用完成；
5. 每个跨区动作都保留逐区结果、重试和审计信息。
