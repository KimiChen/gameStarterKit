# FairyGUI 编辑器插件占位

本目录当前只有 `.gitignore` 和本说明，没有跟踪任何 FairyGUI 编辑器插件，也不构成插件能力承诺。
如后续确需加入设计期插件，应在这里记录来源、版本、启用方式和本地验证方法。

这里不是 Cocos 的 FairyGUI 运行时；后者位于 `apps/Cocos/extensions/fairygui-cc`，由根依赖锁定与
校验流程维护。
