<!-- AUTO-GENERATED by apps/server/tools/feature-codegen/cli.ts from features/<dir>/feature.json. Do not edit. -->

# 能力索引（features 生成）

由 `npm --workspace @game/server run codegen:features` 从 `features/<dir>/feature.json` 生成。
状态字段只描述可机检的**结构状态**（词汇表仅 `planned` / `registered` / `source-present` 三值），
不代表测试实跑或人工验收结论；实跑证据、开放问题与完成判断由人工维护在
`docs/inventory.json` 的 `routeOfTruth.corePlan` 指向的当前计划文件里（本生成器⛔ 不写任何 plan-*.md）。

- `registered`：feature.json 有效且已并入本次生成的客户端 catalog；
- `source-present`：capability fragment 声明的 `defaultEntry` 在仓内存在；
- `planned`：capability fragment 已声明但其 `defaultEntry` 尚不存在。

额外能力的政策、边界与非承诺说明见 [docs/EXTRAFEATURES.md](EXTRAFEATURES.md)；
capability fragment 的合并规则由 `npm run verify:inventory` fail-closed 校验。

## feature 目录

| feature | category | 状态 | 权威文档 |
| --- | --- | --- | --- |
| `builtin` | core | registered | [docs/CLIENT.md](./CLIENT.md) |

## capability fragment

（当前没有 feature 声明 capability fragment。）

