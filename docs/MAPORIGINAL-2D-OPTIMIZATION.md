# mapOriginal 2D 素材、配置与加载改进方案

> 建单：2026-09-24。审计工作区 HEAD：`04bb5e60eaf1`；地图四档 LOD 实现基线：`3084fa00`。
> 同日补充素材构成审计，工作区 HEAD：`8330dbf86f3a`；18 张源 PNG 与 Cocos 镜像逐文件哈希一致。
> 文档身份：改进方案；建单时仅完成只读审计和方案整理，各批实施状态见 §9。
>
> 原版机制与证据等级继续以 [MAPORIGINAL-2D.md](MAPORIGINAL-2D.md) 为唯一真源；
> 本文只规定宿主的素材组织、加载与性能改进，不把这些优化反推为原版行为。
> 既有 M/N 批次保留在 [原施工单](MAPORIGINAL-2D-PLAN.md) 与 [交接单](MAPORIGINAL-2D-NEXT.md)。
> **本方案实施状态只在 §9 更新，不进 plan-v5**；完成后将实际行为回写 kit README，
> 涉及原版对齐结论的变化再回写机制文档。预计收益不能提前写入完成记录。

## 1. 目标与范围

在保持现有地图位置、资源等级、完整 prefab、层内遮挡和四档 LOD 策略的前提下，减少源纹理驻留、
无效图集面积、首屏等待和大型表现配置对脚本包的占用。按以下顺序实施：

**冻结可复测基线 → 图集去重重排 → 带定位元数据的透明裁边 → 分组加载与释放 → GPU 压缩 → 大配置外置。**

静态/动画几何更新优化列为后续 O6。它不是图集和配置改造的退出前提。

本轮不调整原版格值、地图坐标、pivot、父子变换、资源等级和地貌选件规则；不新增 AOI、领地、军队、旗帜或 3D 地图。
原始 ELP、KTX、prefab 和反编译资料保持仓外只读；派生产物和九字段授权台账继续走已有工具。
不额外降低图片分辨率，不抽稀资源件、不删动画帧，不把全地图各级细节全部预烘为大型切片包。
减量分别统计独立图片、物理存储矩形、PNG 包体积与纹理驻留；图集文件数不作为单独的优化目标。

## 2. 当前实现与测量口径

### 2.1 素材管线

```text
仓外原包：KTX / 图集 XML / prefab / 地图字节 / 配置
  → tools/maporiginal-assets：解码、切片、展开节点、缩采样、重打包
  → apps/kits/mapOriginal/data/maps/s1：派生图片、二进制、核验元数据
  → apps/shared/src/kits/mapOriginal/content：生成的内容 TS
  → apps/Cocos/assets/resources/kits/mapOriginal/maps/s1：运行时素材镜像
  → MapoArtResources：resources.load + 引用持有
  → 可见区查询、prefab 求值、Mesh 合批 / 分块烘焙
```

kit 数据目录和 Cocos 资源目录是源产物与镜像关系，不能简单相加后声称运行时加载了两份。
shared/client/Cocos 脚本镜像分别通过 `sync:shared`、`sync:client` 更新，禁止手改镜像。

主画面使用 MeshRenderer + UIMeshRenderer，按固定层容器和层内画家序显示。
离屏分块使用隔离 layer、正交相机和普通 MeshRenderer，避免 UI 队列共享投影造成的错误。
运行时没有为每个地块建立独立 Cocos Sprite；prefab 节点图是数据，由纯逻辑展开为精灵几何。

定位仍取原版 `position / size / pivot / scale / angle / skew / mirror` 及父子变换。
存储像素已有缩采样：资源件限制在 256×192 内；城池和 top 装饰约 0.4 倍；道路约 0.5 倍。
**存储像素尺寸与世界显示尺寸是两个概念**，后者不能从裁后图片或图集格尺寸反推。

代码入口：[资源加载](../apps/client/src/kits/mapOriginal/view/MapoArtResources.ts)、
[网格批次](../apps/client/src/kits/mapOriginal/view/MapoMeshBatch.ts)、
[prefab 求值](../apps/client/src/kits/mapOriginal/logic/mapoScene.ts)、
[安装工具](../tools/maporiginal-assets/install_to_kit.py)。

### 2.2 当前图层与 LOD

| 层位 | 层 | L0 实时显示方式 | 中远景去向 |
|---:|---|---|---|
| 90 | plate | 根隐藏，后台预热 L1 可见分块 | L1/L2 分块纹理；L3 单张同源概览 |
| 100 | terrain | 10×10 格一块，256² 草纹整数次 REPEAT | 烘进分块与概览 |
| 110 | blocks | 沙底→沙 top→雪底→雪 top，原版多边形和手摆件 | 烘进分块与概览 |
| 300 | region | 55,127 个原始锚点＋3,942 个补件，按地貌选择山体 | 烘进分块与概览；流畅画质的细节门控按现状保留 |
| 900 | road | 42,018 条路片摆放，地貌换皮、UV 翻转 | 烘进分块与概览 |
| 1400 | grid | 原版线股贴图，按可见格合批 | L1 起关闭 |
| 1600 | river | 102 条几何、31,140 片摆放；普通及以上画质使用颜色蒙版、法线水流；上叠河岸 top | 静态填充色与河岸件烘入分块及概览 |
| 3400 | decor | 135 个完整资源 prefab，按原始格值、等级、地貌选件 | L1 静态烘焙；L2/L3 不画资源件 |
| 3900 | city | 15 套城池件复用于 249 个城址 | L1 仍独立绘制；L2/L3 改屏幕标记 |
| 3950 | banner | 未实现 | 本方案不新增 |
| 4000 / 浮层 | label | 固定屏幕字号、按优先级避让 | L2 郡名及重要城名；L3 大区名与重要标记 |
| 独立 UI | selection / minimap | 原版 2080 八片选框与动画对齐逻辑格心；缩略图独立显示 | 依现有逐格操作门控显示 |

地名实际挂在 UI root，不随 world 缩放；4000 是逻辑层序登记。
常规画质的标称档界维持 L0 `[0.55,2]`、L1 `[0.20,0.55)`、L2 `[0.04,0.20)`、L3 `[scaleMin,0.04)`，
8% 双向滞回不变。原版 2D 没有这套 LOD，这是宿主策略。

L1 的块边长为 1024 世界单位，L2 为 4096；通常烘成 256²/512²，按预算可继续降采样。
每帧至多一块，块外保留 2 texel 邻域，缓存上限 48 MiB，含 RGBA8、深度附件与在建块。
**中远景水面使用静态填充色，并非近景水流 shader 的冻结帧**；本方案记录这个边界，不顺带改变水面表现。

依据：[层表](../apps/client/src/kits/mapOriginal/logic/mapoLayers.ts)、
[静态场景](../apps/client/src/kits/mapOriginal/logic/mapoStaticScene.ts)、
[缓存预算](../apps/client/src/kits/mapOriginal/logic/mapoLodCache.ts)。

### 2.3 文件统计与内存估算

以下统计来自上述基线的 S1 文件。MiB = 1,048,576 B，KiB = 1,024 B。

| 项目 | 基线 | 口径 |
|---|---:|---|
| 运行时资源目录 | 35 文件，20,124,431 B，约 19.19 MiB | 排除 `.meta`；包含图片、二进制、effect、两个 info JSON |
| PNG | 18 张，约 15.78 MiB | 文件大小，不是纹理内存 |
| 实际加载的 PNG | 17 张，RGBA8 估算约 151.13 MiB | 排除未被当前 loader 使用的 `minimap-mask.png`；无 mipmap |
| 二进制配置 | 13 文件，3,494,841 B，约 3.33 MiB | terrain、摆放表、几何和城池件库 |
| 二进制 gzip 试算 | 约 1.60 MiB | 各文件独立 gzip level 9 后求和，非已发布下载量 |
| 内容 TS | 16 文件，1,382,838 B，约 1.32 MiB | 源文件字节；不等于构建后 JS 体积或 JS 堆 |
| 分块缓存 | 上限 48 MiB | 与上面的源纹理另外计算；8 MiB 概览已在 151.13 MiB 内 |

纹理估算采用 `width × height × 4`。它不含 JS 对象、网格、图片解码暂存、宿主 UI、驱动开销；
也不是对整页 GPU 实测的替代。进入 L3 清掉分块缓存，但当前全部源图集依旧由页面持有。
PNG `.meta` 没有启用地图专用压缩预设，纹理 mipfilter 为 none。
项目虽有 `3d-default / 3d-alpha` 压缩预设，地图图片未绑定，不能算作已启用。

内容 TS 中较大的文件为 `decor.data.ts` 约 547.9 KiB、`terrain.data.ts` 约 365.9 KiB、
`bands.data.ts` 约 230.3 KiB。显示层 `terrain.bin` 原始 2,250,008 B，gzip 试算 1,091,106 B。
旧 README 中的 157 KB、111 KB 等历史数字不作为本轮基线；O0 更新与本方案相关的现状说明。

### 2.4 素材构成与图集空间试算

#### 2.4.1 图片、逻辑件与物理存储

以下为补充审计实测的素材构成；RGBA8 列仍是无 mipmap 估算。

| 图片 / 图集 | 构成与复用 | 当前尺寸 | RGBA8 MiB |
|---|---|---|---:|
| decor | 135 个资源 prefab，320 张切片 | 4096² | 64 |
| region | 26 个山体逻辑条目，19 张不同图片 | 2048×4096 | 32 |
| river-top | 79 张切片供 102 个组复用 | 2048² | 16 |
| city | 158 张切片组成 15 种城址，摆放 249 次 | 1024² | 4 |
| road | 基础 / 雪地共 36 个条目，实际 18 张不同图片 | 1024² | 4 |
| snow-top | 8 张切片供 52 个组复用 | 1024² | 4 |
| desert-top | 5 张切片供 51 个组复用 | 512² | 1 |
| river-mask | 全图水色数据，非单通道灰度遮罩 | 2048² | 16 |
| overview | 同源地貌概览 | 2048×1024 | 8 |
| minimap | overview 缩制，包含上下透明留白 | 512² | 1 |
| ground / snow / desert-base | 三张独立 REPEAT 底纹 | 各 256² | 0.75 |
| river-normal | 重复采样的水面法线，当前 shader 使用 RG | 256² | 0.25 |
| choose | 3 张实际切片组合 8 个选框部件 | 512×64 | 0.125 |
| grid-line / river-fill | 格线与三种静态水色 | 8×8 / 6×2 | 约 0.0003 |
| 合计 | 当前加载 17 张 PNG | — | 151.1253 |

八张含切片的图片（七张地图图集和 choose）共登记 **640 个逻辑引用、635 个不同物理矩形、
610 张 RGBA 完全不同的图片**。25 个冗余矩形来自山体 7 个、道路 18 个。
选框的 8 个部件已经共享 3 个物理矩形，不能把这 5 次复用再算成待去重收益。

道路 18 对基础 / 雪地切片在当前 200×100 存储分辨率及原始 400×200 切片下均逐像素相同；
切片存证中的原图集页、区域、旋转与原画布信息也逐对相同。当前 `build_roads.py` 按两套路径
分别装箱，重复存储了这些像素。O1 保留两套逻辑条目，只合并图片存储。

未发现跨图集的相同切片。decor、city、三个 top 图集内部也没有新增的完整 RGBA 或水平/垂直翻转重复项。
当前地图的三种地貌各 45 个资源 prefab 均有格值引用；320 张资源切片通过完整节点、帧表和纹理轨道可达。
河岸 102/102、雪地 52/52、沙地 51/51 个组及城市 15/15 个件都被摆放表引用，相关切片全部可达。
这证明存在数据引用，未证明每个实例始终可见，也未穷举城市遮挡；不能据此将等级、子件或帧图作为闲置素材删除。

依据：[资源打包](../tools/maporiginal-assets/pack_decor.py)、[道路打包](../tools/maporiginal-assets/build_roads.py)、
[top 打包](../tools/maporiginal-assets/build_tops.py)、[城市打包](../tools/maporiginal-assets/build_cities.py)
及 kit 数据目录中的对应 info JSON。

#### 2.4.2 资源图集的大画布与动画

320 张资源切片中，185 张被帧动画或纹理切换轨道引用；其余 135 张为其他切片。
分类按纹理引用确定，**不代表其他切片的节点没有位移、颜色或父级动画**，不能据此跳过节点更新。
动画相关来源为粮地路径下 169 张、烟雾 15 张、其他效果 1 张；`-1` 空纹理编号不计入图片数。

| 分类 | 张数 | 当前切片矩形面积折合 RGBA8 | 含源护边裁边后的矩形面积折合 RGBA8 |
|---|---:|---:|---:|
| 动画帧 / 纹理切换依赖 | 185 | 31.56 MiB | 1.26 MiB |
| 其他切片 | 135 | 13.61 MiB | 13.55 MiB |

这是切片面积之和，**不含装箱空隙，不是最终纹理内存**。动画相关切片占当前切片矩形面积的 69.87%，
主要减量来自小内容周围的大透明画布。完整帧序、原画布与锚点必须保留。

基础 / 雪地 / 沙地 prefab 分别引用 230 / 214 / 217 张图片，其中 170 张三者共用。
现状已有跨地貌复用，不能把 320 张图片直接按三套独立素材估算或除以三。

#### 2.4.3 含护边的装箱候选

“非透明占比”指 alpha > 0 的像素数 / 整页像素数，不等于直接可回收比例。
本次补充试算保持现有存储分辨率，完整 RGBA 相同的切片先去重；裁边取 alpha > 0 包围盒，
向外保留最多 2 px 原图护边（不超出原画布），再给每个矩形的每边预留 2 px 装箱空间。
使用 MaxRects BSSF 与多种确定性排序，不旋转；成功布局已检查不重叠、不越界。
仍未生成运行图集，也未验证边缘填充、实际采样、GPU 压缩块和引擎画面，因此只是**尺寸候选**。

| 图集 | 当前尺寸 / RGBA8 | 非透明占比 | 候选方式与尺寸 | 候选 RGBA8 |
|---|---|---:|---|---:|
| decor 单页 | 4096² / 64 MiB | 13.4% | 含源护边裁边，本次单页试排为 2048×4096 | 32 MiB |
| decor 两页 | 同上 | 同上 | 其他切片 2048² + 动画相关切片 512×1024；需分页支持 | 18 MiB |
| region | 2048×4096 / 32 MiB | 25.7% | 26 个条目共享 19 张图片，去重紧排到 2048²，无须裁边 | 16 MiB |
| city | 1024² / 4 MiB | 21.5% | 158 张切片原样重排到 512×1024 | 2 MiB |
| road | 1024² / 4 MiB | 10.8% | 36 个条目共享 18 张图片，无裁边 512×1024；追加裁边 512² | 2 / 1 MiB |
| river-top | 2048² / 16 MiB | 9.6% | 含源护边裁边后 1024² | 4 MiB |
| snow-top | 1024² / 4 MiB | 13.5% | 无裁边 512×1024；含源护边裁边 1024×256 | 2 / 1 MiB |
| desert-top | 512² / 1 MiB | 43.3% | 本次未找到页尺寸下降 | 1 MiB |

**修订此前 decor 2048² / 16 MiB 的估算口径**：初次试算未包含源图护边。
本次全部护边矩形连装箱间隔共 4,129,271 px，2048² 只有 4,194,304 px，需要约 98.45% 利用率；
单页试排未成功，但这也不证明数学上绝不可能。16 MiB 保留为待证目标，不能作为确定收益。
两页 18 MiB 已通过矩形试排，仍须比较分页的材质切换、draw call 与遮挡正确性。

若采用 decor 两页 18、region 16、city 2、road 无裁边 2、river-top 4、snow-top 1 MiB，
其余纹理保持，源纹理候选合计 **70.13 MiB**；再做 §2.5 小地图留白裁减为 **69.63 MiB**。
道路追加裁边可在此基础上再减 1 MiB；单页/两页、道路两种布局、雪 top 两种布局分别互斥，不能重复累加收益。
这些数字不含分组释放和 GPU 压缩，不含 48 MiB 分块缓存；PNG 下载体积要用真实产物另测。
O0/O1/O2 完成前，不能把上述估算或初版四项合计约 79 MiB 的估算记为已兑现收益。

本次临时证据位于 `.cache/maporiginal-image-composition/`：`report.json` 含 PNG 指纹、切片哈希、
引用与装箱坐标，`audit.py / reachability.py / page-trial.py` 可复核；该目录被忽略，不随仓库交付。
本文保留关键方法和数字，O0 仍需将统计能力整理为受维护的入口并补真实性能基线。

### 2.5 小地图、数据纹理与小图片的减量边界

**小地图**：当前 `minimap.png` 为 512²，alpha 包围盒是 `[0,128,512,384]`，
由 `bake_overview.ts` 把 overview 缩成 512×256 后垂直居中。O2 优先原样保存这块内容，
以 §4.3 的布局契约保持画面与导航一致，纹理候选从 1 降为 0.5 MiB。
与 overview 共用纹理、取消独立 minimap PNG 是另一个备选，理论可少持有 1 MiB，
但直接缩放与当前预缩小图的采样不同；须比较锯齿、抖动和可读性。
如为此新增整张 overview 的 mip 链，额外内存可能超过节省量，不能预先计为净收益。

**河流水色图**：`river-mask.png` 为 2048² / 16 MiB，含 17,347 种 RGB 值，三通道不同，
不是可直接替换成 R8 的灰度遮罩。83.59% 像素 RGB 为黑色，但分散在全图内部；
另有 142,612 个 alpha 为 0、RGB 非黑的像素。[当前水面 shader](../tools/maporiginal-assets/shaders/mapo-river.effect)
读取 RGB、输出 alpha=1，**alpha=0 不能作为可删像素的判据**。
优先沿 O3 按需释放与 O4 专用 GPU 压缩处理；稀疏分页须增加映射、采样和邻接护边，暂不排入本轮。

**小图片**：`river-fill.png` 为三个 2×2 平色块，文件 89 B；`grid-line.png` 为 8×8、两种 RGBA 值，文件 91 B。
可研究用常量或生成纹理表达，但本轮容量收益很小。选框已经复用切片，三张 256² 底纹合计仅 0.75 MiB。
未加载的 `minimap-mask.png` 只有 1,995 B，移出运行包不能算作当前加载显存的收益。

依据：[概览与缩略图烘焙](../tools/maporiginal-assets/bake_overview.ts)、
[小地图 UI](../apps/client/src/kits/mapOriginal/view/MapoMinimap.ts)、[水面导出](../tools/maporiginal-assets/build_surface.py)。

## 3. 已确认的问题

| 编号 | 问题与影响 | 处理阶段 |
|---|---|---|
| P01 | 源图集全部加载；低画质和 L3 仍保留用不到的纹理 | O3 |
| P02 | 资源动画帧、河岸透明留白大；山体 7 个与道路 18 个矩形重复存储；城市、雪 top 等固定方形图集浪费面积 | O1 / O2 |
| P03 | loadMapoArt 分多组等待，组内部分资源仍串行；概览也要等整套结果返回才交付页面 | O3 |
| P04 | 关闭页面释放引擎资源，但 displayCells、bands、道路 DataView、城池展开对象等仍被模块引用 | O3；是 CPU 数据驻留，不据此断言每次重开无限泄漏 |
| P05 | 未配置地图 GPU 压缩，源纹理内存主要由展开像素面积决定 | O4 |
| P06 | 客户端表现大表混在生成 TS；大规模内容变更与脚本发布耦合 | O5 |
| P07 | 二进制各用不同计数/尺寸头，缺少统一运行时版本绑定；目前有长度/数量检查，不能说毫无校验 | O3 / O5 |
| P08 | `minimap-mask.png`、两个图集 info JSON 进入 resources，当前地图代码未消费；增加发布冗余 | O0 核全仓/构建引用后，O3 移出运行时包 |
| P09 | decor/top 只要有动画，就会逐帧重新展开、排序、上传同层可见几何 | O6 |
| P10 | 小地图 512² 画布只有中间 512×256 承载内容，透明留白也占纹理面积 | O2；复用 overview 另作采样对照备选 |

`MAPO_LAYERS.streamed` 当前只有登记字段，不能将其解释成已经实现空间分块下载。
地形查询的二分裁剪、当前 Mesh 合批和 48 MiB 缓存继续保留。

## 4. 图片数据契约

### 4.1 逻辑件与图片条目分离

同图复用只合并像素存储。逻辑件仍保留独立的资源值、variant、prefab、pivot、transform、颜色、层序和动画。
用稳定 `textureId` 引用图片条目，图集坐标由布局表解析；不得把格值直接用作可复用纹理的唯一身份。
去重需比较实际像素、尺寸与采样约定；路径可用于识别候选，不能仅凭名字相同合并不同像素。
道路基础 / 雪地的不同来源路径应能映射到同一图片条目，仍保留两套 clientResId、选件规则与 UV 镜像。

图片条目至少包含以下字段，O1/O2 在单一 schema 中生成：

| 字段 | 含义 |
|---|---|
| `textureId / atlasId / rect` | 稳定图片身份、图集身份、有效 UV 矩形 |
| `nativeSize` | 原始图片画布尺寸，用于来源校验 |
| `storageSize` | 已有缩采样之后、裁边之前的完整像素尺寸 |
| `trimRect` | 相对 storageSize 左上角的裁剪矩形，含保留的采样护边 |
| `layoutVersion / contentHash` | 布局版本与内容绑定；不改变 prefab 的 pivot |

`source` 真路径和转换说明留在可审计元数据中，不必把长路径重复带入每个运行时节点。
优先在每个现有图集族内重排；多图集分页仅在内存/平台上限或地貌按需加载收益明确时引入。
分页不能按 atlasId 全局重排透明精灵，应保持原来的画家序及跨页提交顺序。
O2 对照 decor 单页与“其他切片 / 动画相关切片”两页方案；同一切片被多类节点引用时仍只存一份。
两页只是纹理布局，不能用来判定节点是否静态；实时和离屏烘焙都按原序生成连续纹理批次，
不把全部动画提前或延后绘制。布局验收需同时报告内存、draw call、材质切换及重烘结果。

### 4.2 裁边后的定位

保留原节点 `size = (Nw, Nh)`、`pivot = (px, py)`、锚点和完整父矩阵。
设 storageSize 为 `(Sw, Sh)`，trimRect 为 `(tx, ty, tw, th)`，像素原点在左上，节点局部 y 向上：

```text
xLeft   = Nw × (tx / Sw - px)
xRight  = Nw × ((tx + tw) / Sw - px)
yTop    = Nh × (1 - ty / Sh - py)
yBottom = Nh × (1 - (ty + th) / Sh - py)
```

先生成这四个局部点，再走原本的父子矩阵、skew、旋转、缩放和世界换算。
**不能把裁后宽高直接替换原 size 并继续套原 pivot**，也不能重新按图片底边定位。

当前镜像通过翻 UV 实现。裁边后水平镜像的几何范围应使用 `tx' = Sw - tx - tw`，
垂直镜像应使用 `ty' = Sh - ty - th`，同时保留正确的 UV 方向；需覆盖非中心、越界 pivot 与双向镜像。
帧动画每帧独立保存裁边信息，共享同一原始节点锚点，换帧不能跳动。

仅裁确定不参与显示的透明区；不得用 alpha 阈值删除半透明阴影。
保留线性采样需要的透明护边和边缘 RGB，明确 padding/extrude 规则，禁止再次以粘贴蒙版乘 alpha。
运行时 shader 的乘色/加色与透明规则改变时，必须重新验证可裁剪条件。
GPU 压缩、mipmap 所需的护边另行验证，不能用当前 2 px 间隔直接推断所有级别都安全。

256² 草/雪/沙重复底纹继续独立存放并保持 REPEAT、整数周期和 POT；不塞进普通裁边图集。
颜色蒙版、法线图、格线、选框和概览不参与第一批通用 alpha 裁边。
每类图片登记 shader 实际读取的通道、alpha 用途与采样方式；水色图按 §2.5 保留有效 RGB，
不能把透明精灵的裁剪规则自动套到数据纹理。

### 4.3 小地图裁边与导航契约

小地图作为独立 O2 子项验证，不套用 prefab pivot。保留未裁画布 `sourceCanvasSize=[512,512]`
与 `contentRect=[0,128,512,256]`；新存储图为原样截取的 512×256，不再次缩采样。
UI 导航区域仍为正方形，内容图片居中显示、宽度不变、高度为导航区域的一半；
新纹理 UV 覆盖整张存储图，不能再把原画布中的留白比例当作新纹理的 UV 裁切范围。
世界坐标映射、点选与视口框共同遵循这个内容矩形，避免只改 PNG 导致内容拉伸或点击错位。
检验中心、四个地图边界、上下留白点击和视口框；记录原图与新图的内容像素逐字节一致。

## 5. 加载、生命周期与配置组织

### 5.1 资源组和需求

先在现有 resources 路径下拆加载生命周期，再迁移 Asset Bundle，避免同时改引用、布局和包路径。
加载复用框架 [AssetLease](../apps/client/src/view/scene3d/AssetLease.ts) 与
[Cocos loader](../apps/client/src/view/scene3d/cocosAssetLoader.ts)，不重写另一套引用计数、取消和超时系统。
它是通用资产加载件，消费它不要求地图改成 3D 舞台。

| 资源组 | 内容 | 持有条件 |
|---|---|---|
| overview | 概览、缩略图、基础标记与可先显示的地名数据 | 页面打开即加载，首先交付 |
| geography | 重复底纹、沙雪多边形、山体、道路、静态河流及 top 数据 | L0 显示、L1/L2 补烘任务需要 |
| resources | 资源 prefab、资源图集、显示层格值及相关地貌判定 | 非流畅 L0、需要资源件的 L1 烘焙；点选数据另独立持有 |
| cities | 城池模型图集与件库 | L0/L1；远档标记不依赖完整城市图集 |
| effects | 水流 effect、河色蒙版、法线图、格线、选框 | 按画质、LOD 和选择状态分别需求加载 |

逻辑分组不意味着马上复制或拆开每张物理图集。共享图片只加载一份，先支持按 LOD 分组；
按地貌分页在 O1/O2 后比较 draw call、边界混合场景与内存收益再决定。
L1/L2 缓存不是免加载源素材：补块时必须持有对应源资产，完成后才可按策略归还。

需求状态至少有 `idle → loading → ready → releasing`，失败可重试；只有同代际、同地图版本的结果可安装。
页面先展示 overview，组未齐时保留同源底图；不能把缺源烘出的半张块标成 ready。
新路径下硬错误需保留资源地址和错误码，不再无声吞掉解析失败；UI 是否可继续由组的必需性决定。

出档释放可设短宽限避免来回捏合反复加载，具体时长在 O0/O3 实测后冻结；关闭页面立即取消在途请求。
已经成功的租约须显式 release；取消 signal 不代表可立即释放仍在渲染的资产。
节点、材质、烘焙任务和帧末 GPU 提交不再引用后，才能归还纹理租约；保留迟到回调与幂等释放保护。
烘焙器目前会持有源材质，进入不需要源图的状态时必须一起解除这类引用。

### 5.2 地图数据生命周期

新增地图级数据持有者（建议名 `MapoDataStore`，尚未实现），显式持有显示格、地貌带、摆放表、几何库与展开件。
View 负责加载和注入，Logic 只消费纯数据；关闭或切图时解除引用。不能让模块单例永久保留上一张地图。
若保留跨页面 CPU 缓存，应有独立预算、版本 key、淘汰和报告，不能把它记入纹理缓存的 48 MiB。

缓存身份至少绑定 `mapId + contentVersion + atlasLayoutVersion + lod + pixels + quality/details + tile`。
版本或布局切换应取消旧烘焙、作废旧块，源图片、UV、配置必须作为同版本集合安装；不在一个帧里混用两版。

### 5.3 Bundle 与配置存储

O3 的独立地图包候选根为 `apps/Cocos/assets/bundles/kit-mapOriginal-s1/2d/`，bundle 名为 `kit-mapOriginal-s1`。
O3-B2 实施前核对安装工具、宿主包归属与 Creator 导入规则后冻结；新路径未创建前不得写成现状。
包内按上述逻辑组组织，是否放远端由平台构建配置决定，不在代码拼 CDN 地址。
切换后更新安装器、确定性 UUID、加载路径、台账和机检，不在 resources 中留下第二套可发布副本。

| 数据 | 目标方式 | 理由 |
|---|---|---|
| 格值、山体/道路/河流摆放、几何 | 二进制＋TypedArray/DataView，优先维持现有定长布局 | 稠密查询与现有二分裁剪已适合，不改成海量 JSON 对象 |
| 客户端 prefab 树、轨道、帧表、图集布局 | 按需数据资源；O5 比较外置 JSON 与带字符串表的二进制 | 优先解除脚本耦合，再依据解码时间与堆占用决定格式 |
| 图层顺序、LOD 阈值、小型规则 | 小型 TS 常量和类型 | 易审阅、易检查，外置收益有限 |
| shared 通行层 | 本轮保留纯 TS RLE 入口 | shared 零依赖，服务端不能为此直接读取客户端资源；未来通过宿主注入扩展 |
| 构建来源、校验说明 | kit-only JSON 与授权台账 | 保持审计能力，运行时无需携带全部长路径 |

新增运行时 manifest 记录 mapId、schemaVersion、contentVersion、atlasLayoutVersion、各组依赖与字节数。
二进制新增/升级头时包含 magic、版本、字节序、记录数、段偏移和边界；已有严格长度校验继续保留。
采用稳定 ID、共享字符串/纹理表和明确缺省值；不得以量化体积为由损失 transform、越界 pivot 或动画语义。

内容哈希绑定生成源与布局；平台纹理变体的实际文件校验由构建清单绑定，不能拿源 PNG 的 hash 校验 ASTC 文件。
继续使用 Creator 的版本化资源缓存机制，不另造一套浏览器私有缓存。传输 gzip/Brotli 需在实际发布服务验证
Content-Encoding、缓存和平台支持；压缩源文件的体积不等于最终包或网络流量。

## 6. 分批实施与退出条件

### O0 · 冻结可复测基线

**落点**：`tools/maporiginal-assets/` 新增只读统计入口；扩展 `tools/creator-preview/maporiginal.mjs` 的报告；
校正 kit/工具 README 中与本轮现状直接相关的旧数字与说明。

**交付**：记录文件数/字节、图片尺寸、逻辑引用/物理矩形/独立图片数、逐切片 RGBA 哈希与重复对、
跨地貌复用、完整帧表/纹理轨道依赖、全图数据可达性，以及图片实际读取通道和可裁条件。
区分矩形试排与真实打包产物，分别记录护边、间隔、页尺寸、利用率和采样约定。
记录加载请求、CPU 数据持有量、源纹理与 RT 分项、首个概览/近景可操作时间、帧时和上传量。
报告标注源码/素材哈希、平台、图形 API、DPR、视口和画质。
本地证据统一落 `.cache/creator-preview/maporiginal-optimization/<batch>/`，按仓库忽略规则不入库。
§2.4 的临时脚本与报告只作起点；整理为受维护的工具后重新核验，不能以临时分析代替 O0 交付。

**退出**：相同输入重复统计相同；注册候选 packer 不重叠、不越界断言；按 §7 固定操作路径建立基线。
空纹理 `-1` 不计入素材；同一个物理矩形的多次逻辑引用不算重复存储；动画不能只检查当前帧。
现有 750×1624 为设计分辨率，报告另外记录 CSS/backing canvas 实际大小，不能混用。
现状统计可复测后，才冻结各目标平台的首屏、帧时和纹理预算；不借桌面结果声明手机达标。

### O1 · 无像素裁剪的去重与重排

**依赖**：O0。**风险**：低至中，主要是 UV 和重复件 transform 被误合并。

**落点**：`pack_regions.py / build_roads.py / build_cities.py / build_tops.py`、公共打包/布局 schema、
相关生成数据、`mapoRegions.ts / mapoRoads.ts / mapoCities.ts / mapoTops.ts`、
`verify_fidelity.py`、内容测试与安装器。

**交付**：山体 26 个条目共享 19 张图片、道路 36 个条目共享 18 张图片；城市和雪 top 改紧凑矩形；
建立纹理 ID 与布局映射。维持原存储像素，不改变缩采样。
山体 16 MiB、道路 2 MiB、城市 2 MiB、雪 top 2 MiB 为当前候选；
不满足就记录真实结果与原因，不能隐式缩图凑数。道路追加裁边的 1 MiB 候选留到 O2。

**退出**：有效切片 RGBA 与基线逐像素一致；所有逻辑件变换、件数、层序不变；两次生成字节确定；
UV 无越界、无错片；L0、缓存与重烘概览使用同布局，§7 规定的截图和机检通过。
道路两套 clientResId、地貌选件与水平翻转保持，18 对源切片等价关系有断言；
城市保留全部 15 个件与 249 条摆位，不通过删城墙、建筑或子件达到页尺寸目标。

### O2 · 保留原版定位的透明裁边

**依赖**：O1。**风险**：高，可能重新引入错位、换帧抖动、边缘暗线。

**落点**：`pack_decor.py / build_tops.py / build_roads.py`、`prefabs.types.ts` 的生成契约、
`mapoScene.ts / mapoPrefab.ts / mapoMesh.ts`、`verify_fidelity.py`；
小地图涉及 `bake_overview.ts / MapoMinimap.ts / mapoFar.ts` 与布局元数据。
若采用 decor 分页，同时扩展 `MapoArtResources / MapoDecorRenderer / MapoChunkBaker`、
`mapoStaticScene` 与概览烘焙的纹理页消费；静态烘焙和实时绘制必须消费同一裁边几何逻辑。

**交付**：先资源动画的大画布及河岸，再雪 top 和道路；小地图留白作为独立子项，采用 §4 契约。
资源图集比较单页与两页：16 MiB 单页为待证目标，本次两页 18 MiB 仅通过尺寸试排，
最终选择以像素、画面、内存及批次成本共同决定。每帧保留独立裁边信息，不删帧、不改变播放时间。
小地图优先原样裁去上下留白到 512×256；共用 overview 为采样对照备选，不叠加计收益。
更新原包核验器：比较保留像素 RGBA、裁区 alpha 和采样护边，
不能为了适配裁边而删除现有“不得重复乘 alpha”检查。

**退出**：节点锚点、原始画布尺寸及 world transform 不变；非中心/越界 pivot、mirror、skew、
负缩放、多层父矩阵、半透明阴影、变更纹理轨道和帧动画均通过；选框仍落在同一逻辑格心。
透明裁边之外无像素改写；渲染差异限值按 §7，不以源矩形面积下降代替实际显存下降。
分页保持跨页画家序与换帧纹理引用，L0/L1 和烘焙路径均无缺片；单独报告 draw call 与材质切换变化。
小地图通过 §4.3 的布局、点选与视口框回归；水色图等数据纹理未被普通 alpha 裁边误处理。

### O3 · 分组加载、数据释放与地图包

**依赖**：O2；分组依赖表和通用租约接法可提前设计，路径迁移待 O2 产物稳定。
**风险**：高，涉及取消、迟到、烘焙引用和缓存版本。

| 批次 | 落点与交付 | 退出判据 |
|---|---|---|
| O3-B1 | MapoArtResources、MapOriginalWorldView、MapoFarRenderer/ChunkBaker、各数据 reader；接框架 AssetLease、引入地图数据持有者，先保持 resources 路径 | 概览先交付；组独立加载；取消/超时/失败可重试；旧代际不能安装；关闭后数据和引擎资源解除持有；L3 宽限结束后源材质不再钉住近景纹理 |
| O3-B2 | install_to_kit.py、mapoFar.ts 等地址常量、manifest、台账、内容测试；迁移独立 Bundle、移出无消费者资源 | 主包/地图包体积分开记录；无重复发布副本；布局/数据错版拒绝安装；PNG fallback、加载缺片及离线缓存路径可复测 |

L1/L2 补烘需显式持有源组，已完成块可独立保留；失败保持概览，不能记录半成品为完成。
关闭重开 10 次、加载中关闭、快速切档、连续跨区域平移均有自动化断言和引擎证据。
退出依据包括源组引用和 CPU 数据指标；不能只用“残留 Node 为 0”证明全部内存已归还。

### O4 · 地图专用 GPU 压缩

**依赖**：O3；压缩画质试验可在 O2 后并行准备，不改变正式产物。
**风险**：中，关注半透明边缘、设备格式支持和 fallback 体积。

**落点**：图片 meta 生成策略、Creator builder 的地图专用预设、构建产物检查、真机重放。
源 PNG 保留；普通颜色图集先比较 ASTC 4×4 / 5×5，按发布平台验证可用格式并保留 PNG 回退。
蒙版、法线、格线和选框分别对照，不能直接套 3D 的 8×8 预设。
水色图按 shader 实际读取的 RGB 验收，包括 alpha=0 区域；压缩前处理不得依据透明度清除这些 RGB。
法线图按实际 RG 采样与水流结果评价，不以整张图片的平均颜色误差替代。

ASTC 无 mip 估算为 `ceil(width/blockWidth) × ceil(height/blockHeight) × 16` B；
RenderTexture 仍按自身格式计费，不因源图压缩自动减少。增加 mipmap 会增加存储和显存，本批默认不全局打开。
若另开 mipmap 试验，必须同时验证图集护边、远端串色、REPEAT 接缝和 WebGL1 约束。

**退出**：发布包实际包含预期变体，设备实际选择格式被记录；不支持设备走 PNG 且可显示；
按 §7 给出质量报告与源纹理内存变化。不能以 `.meta` 勾选或编辑器 PNG 预览替代发布验证。
无合适移动设备时，标记真机待验收，本阶段不能退出。

### O5 · 客户端大配置外置

**依赖**：O3 的 manifest、版本和数据生命周期；按主顺序在 O4 后交付。
**风险**：中，异步就绪与 schema 漂移。

**落点**：`pack_decor.py / scene_export.py / build_tops.py` 等导出器、`content/*.data.ts` 消费入口、
数据 reader、manifest 和原包/内容回归。

**交付**：先外置 decor 节点与动画、top 场景、图集布局；试验外置 JSON 与二进制，记录原始/传输大小、
解码耗时和保留堆，再选择格式。保留小型 TS 规则和 shared 通行入口；不得在 shared 中引入 Node、DOM、cc、网络或文件读取。

**退出**：节点树、transform、颜色、纹理引用与关键时间点的求值结果等价；截断、错版本、越界偏移、
非法 ID 明确拒绝；构建脚本体积和解析指标可复测改善。若二进制没有足够收益，可采用外置 JSON，
不能仅以“二进制更先进”作为格式依据。

### O6 · 静态与动画更新优化（后续批次）

**依赖**：O2/O3/O5。**落点**：mapoScene、MapoDecorRenderer、MapoTopRenderer、MapoMeshBatch。
预编译静态节点和局部矩阵；区分静态数据与受父动画影响的数据；只更新变化的几何/属性范围。
需要混合提交时按原画家序拆连续段，不能先画全部静态件再画全部动画件。

**退出**：可见区不变且无动画时不重复展开/上传；有动画时保持帧语义、遮挡和乘加色；
报告 CPU 时间、每帧上传字节和 draw call 的实际取舍，三者不能只挑有利项报告。

## 7. 验证与回退

### 7.1 固定回归矩阵

| 场景 | 检查内容 |
|---|---|
| 草地、雪带、沙带及两带交界 | 选件、同屏混合图集、原始件数、道路连续性 |
| 13 种山形及补件、跨缓存边界大件 | 去重不丢变换，裁边不改变外伸与遮挡 |
| 资源等级 1–10、金矿、城市占格 | 不借等级、不缺子片、不恢复城市内资源件 |
| 河岸动画、阴影、洛阳复杂城池 | 完整节点、颜色、透明边缘、层序 |
| 道路基础/雪地成对切片及双向路片 | 去重后逻辑条目不变、水平 UV 翻转正确、交界连续 |
| decor 跨页交错及纹理切换帧 | 原画家序、页间换帧、L1 静态烘焙正确，记录批次和材质切换 |
| 小地图中心、边界和上下留白 | 内容不拉伸，点选落格与视口框一致，内容矩形采用同一布局 |
| 河流水色图的透明 RGB 与法线 RG | 不误裁/清空 shader 实际采样的数据；压缩前后水色、水流逐项对照 |
| 选框、非中心与越界 pivot、skew / mirror | 同一格心、同一世界位置，动画换帧无抖动 |
| L0↔L1↔L2↔L3 双向及快速往返 | 滞回不变，补块依赖完整，无半成品与错版缓存 |
| 页关闭、加载中关闭、失败重试、重开 10 次 | 迟到结果不会复活旧页；租约、CPU 数据、RT 分项回落 |
| 750×1624 竖版设计尺寸和横版回归视口 | 记录实际 canvas/DPR；缩放到底全图可见；结束后恢复竖版预览 |

布局/裁边阶段在同分辨率、同 shader、固定相机与动画时间下对比旧/新输出。
机检要求逻辑 ID、锚点和 transform 不变，保留像素 RGBA 一致。
同一后端截图比较的限值为每通道 MAE ≤ 1/255、任一通道差值 > 8/255 的像素占比 ≤ 0.1%；
O0 以静态概览重复截图误差为 0 校准，整图及指定的每个边缘/关键物件 ROI 均需通过，
不能靠大面积背景稀释错位；动画场景仍需先对齐相机与动画时间，不能直接比较任意两帧。
缓存接缝与概览另设固定邻接条带/地标检查，不把不同 LOD 的本来差异误判成回归。

GPU 有损压缩独立评价：比较颜色和 alpha 的局部误差、边缘与阴影，并记录质量/内存取舍。
O0/O4 试验后冻结压缩阈值；未通过前继续使用 PNG，不降低裁边阶段的无损验收要求。

### 7.2 命令与证据

现有命令从仓库根运行；Python 需管线的 Pillow/numpy 等依赖与所需仓外只读数据：

```bash
npm run sync:shared
npm run sync:client
npm run typecheck:client
npm run test:client
npm --workspace @game/server run test
python3 tools/maporiginal-assets/install_to_kit.py --check
python3 tools/maporiginal-assets/verify_fidelity.py --map s1
python3 tools/maporiginal-assets/test_asset_audit.py
python3 tools/maporiginal-assets/audit_assets.py --map s1 --out .cache/creator-preview/maporiginal-optimization/current/assets.json
node tools/creator-preview/run.mjs mapOriginal --out .cache/creator-preview/maporiginal-optimization/current
npm run verify:sync
npm run verify:all
```

按批次选择相关命令；只改文档不执行素材安装或原包转换。以上不表示命令已在本方案实施中跑过。
O0 的只读统计、候选装箱断言、截图局部对照及桌面 CPU/GPU 探针已加入独立工具；Python 测试须显式运行，
不冒称 verify:all 覆盖了 Python 或真实引擎。O1 实际装箱像素验证、O3 取消/超时/十次重开的完整生命周期闸、
O4 发布格式验证仍由各批交付。Creator 重放需要本地服务和可见预览，与无头测试互补。

改布局时重新生成图片、布局数据、概览/缩略图与哈希，再运行安装器和台账生成器，最后同步源码镜像。
`bake_overview.ts` 消费 kit 已安装数据，须先准备一致的输入快照，再安装新烘焙产物，避免混用新旧布局。
新增镜像文件须在暂存后检查 `.meta` 完整性；不手改生成数据和授权台账。

每批作为独立本地提交，回退恢复该批对应的代码、布局、图片与 manifest 整组版本。
避免运行时长期保留两套格式互相兜底；迁移兼容仅在明确期限内存在。用户当前要求不 pull、不 push。

## 8. 技术取舍与参考

- **PNG 源图继续保留**：便于确定性重打包与像素核验；发布变体由平台构建生成。
- **WebP 不作为显存方案**：它主要影响包与传输，GPU 压缩纹理才直接改变纹理驻留格式。
- **不盲目采用 KTX2/Basis**：本轮优先走已用 Creator 3.8 的构建/加载链；引入新转码链需单独验证运行时与小游戏兼容。
- **不逐格拆图片请求**：图集族和地图包仍是加载单位，空间分片需以实测收益证明额外索引与请求值得。
- **不全量 JSON 化或二进制化**：按大小、访问模式、共享边界选择；当前 225 万格的一字节显示层继续保留。
- **不改变水面和既定采样精度**：中景静态水面与原图缩采样是已知表现边界，恢复更高精度另立可对照目标。
- **S2 原创素材另按作者态设计**：独立图片、组合件和地图实例分层；环境覆盖、主体、等级附加件及动画部件
  可在新美术制作时分开，以增加复用。不能将此当作现有 S1 合成 PNG 可自动无损拆解的证据，也不扩大本轮实施范围。

外部机制参考（2026-09-24 查阅；支持矩阵仍以目标设备实际选择的格式为准）：

- [Cocos Creator 3.8：Compressed Textures](https://docs.cocos.com/creator/3.8/manual/en/asset/compress-texture.html)：
  构建多格式纹理、按设备选择、PNG/WebP 与 GPU 压缩的内存区别、通用格式回退。
- [Cocos Creator 3.8：Asset Bundle](https://docs.cocos.com/creator/3.8/manual/en/asset/bundle.html)：
  按需资源包、远端/本地/小游戏子包及版本组织。移动到 Bundle 不等于已经做到按需加载。
- [本仓客户端文档](CLIENT.md)：Logic/View 边界、通用 AssetLease 的取消/失败/迟到释放语义。

## 9. 实施状态（唯一登记处）

| 批次 | 状态 | 提交 / 证据 / 实际收益 |
|---|---|---|
| O0 基线与统计 | 已完成（桌面基线） | 受维护统计、装箱断言、截图 ROI 对照、请求/CPU/纹理/RT/帧时/WebGL 上传探针已交付；横竖版地图各 20 步通过、两次静态统计字节相同、重复概览 PNG 像素相同。有效证据与预算边界见下方记录 |
| O1 去重重排 | 已完成 | 山体 26→19、道路 36→18 物理图片；四张图集共减少 22 MiB。原包/基线像素、固定时间渲染、横竖预览及隔离快照 verify:all 全过；详见下方记录 |
| O2 定位安全的裁边 | 未开始 | 含动画画布、分页对照、河岸/雪 top/道路与小地图留白；待真实打包和画面验收 |
| O3-B1 分组租约与数据生命周期 | 未开始 | — |
| O3-B2 Bundle 与版本绑定 | 未开始 | — |
| O4 GPU 压缩 | 未开始 | — |
| O5 大配置外置 | 未开始 | — |
| O6 动画更新优化 | 未开始 | 后续批次，不阻塞前五项 |

阶段退出时记录源码提交、产物指纹、命令退出码、设备/图形 API、证据目录和前后分项统计。
仅完成代码、只有桌面截图或只有装箱试算时，保持对应验收项未完成。

### O0 · 2026-09-24 实施记录

源码与本节同批提交。证据根为 `.cache/creator-preview/maporiginal-optimization/o0/`，
`assets.json` 与 `assets-repeat.json` 逐字节相同，输入指纹为
`b3a57940c09624b2d5e204c99ea9fb3469e780a9dc91b1c346bc97e744e63384`。
预览报告另存源码、素材与工具哈希，并核对八个数据读取器的真源、Cocos 镜像和 Creator 编译源码一致；
仅凭工作区 HEAD 不能证明未提交代码已被引擎加载。

本批新增受维护的只读统计入口、确定性矩形试排、整图及局部 ROI 对照，以及桌面预览探针。
数据读取器只增加不触发解码或懒加载的统计接口；地图逻辑、布局、图集和缩采样均未改变。

| 项目 | O0 核验结果 |
|---|---|
| 运行时文件 / PNG / 二进制 / 内容 TS | 35 文件 / 18 PNG / 13 二进制 / 16 TS；字节数与 §2.3 一致 |
| 素材构成 | 640 个逻辑引用、635 个物理矩形、610 张族内不同图片；待合并 25 个冗余矩形 |
| 动画依赖 | decor 的 320 张图全部可达；185 张参与帧表或纹理切换，包含完整帧依赖及 `-1` 空纹理处理 |
| 请求与源纹理 | 打开地图加载 32 项：17 张纹理、13 个 BufferAsset、2 个 effect；源纹理 RGBA8 合计 158,466,352 B（151.1253 MiB） |
| LOD / 画质持有量 | L3 和流畅画质仍持有上述全部源纹理；这是 O3 的优化对象，本批未减少驻留 |
| 分块 RenderTexture | 含深度及在建块不超过 48 MiB；L3 清为 0 |
| 关闭页面 | 源纹理、RT、有效 BufferAsset 载荷和地图节点均回到 0；读取器仍持有 7,792,798 B 数组及 1,642 个城市精灵、1,899 个 top 记录 |

数组持有量与 BufferAsset 存在别名，不能相加；对象数未换算成推测的 JS 堆字节。
关闭后残留的数据是已确认的模块缓存，完整释放交给 O3；一次关闭/重开不替代该阶段的十次重开与迟到回调验收。

原包核验 `verify_fidelity.py --map s1` 通过：381 个入口、4,504 个展开节点、635 个切片，
替代项为 0、RGBA 不同像素为 0。新增只读接口改变了概览绑定的源码指纹，因此通过
`bake_overview.ts` 和安装器重生成 `overview.info.json`，未手改指纹。
几何哈希仍为 `35577d83f08537c4b0afdb2a13f7f051b63326066603b33839359df3056f84be`；
`overview.png` / `minimap.png` 文件和 RGBA 均与本批前完全一致。

`npm run verify:all` 退出 0，包含客户端 1,348 项、服务端 1,395 项、UniFlex 80 项及其余核心校验。
Python 审计测试另跑 7 项通过，安装镜像检查通过；不把它们算作 npm 校验隐含覆盖。

桌面有效基线使用 Apple M4、Chrome 153、ANGLE Metal / WebGL 2、DPR 1，设计尺寸均为 750×1624。
`portrait-baseline/` 完整运行 26 步、`landscape-isolated/` 25 步通过；差别来自外层预览启动步骤，
地图本身均覆盖 20 步、14 个有效采样窗口。横屏洛阳在 L1 检查完整城池与城名，竖屏在 L0；
草地、雪地、沙地、四档往返、GPU 邻块接缝、四种画质、关闭及正常入口重开均有证据。

| 指标 | 竖版，沿用网页设备选项 | 横版回归 |
|---|---:|---:|
| CSS canvas | 393×772.90625 | 1624×750 |
| backing canvas | 393×773 | 1624×750 |
| 第一帧近景地表 | 557.3 ms | 690.9 ms |
| 点选已确认的保守时间上界 | 5,839.8 ms | 6,285.3 ms |
| 各窗口帧间隔 p95 最大值 | 22.5 ms | 18.7 ms |
| 各窗口每帧上传量 p95 最大值 | 760,252 B | 295,392 B |

首近景从点击入口前开始计时，表示 Mesh 已画出；点选上界包含两段 2.2 s 被动采样、
自动化轮询和截图，**不是输入响应延迟**。首概览约 9.5 / 11.0 s 才出现，是操作进入 L1 的时间，
不能作为“概览首屏加载慢”的证据。预览缓存状态为 warm/未清缓存，未测首装网络时延。
每窗口至多保留最近 120 帧，上传量与 draw call 包含同 WebGL context 的 UI。

桌面回归先冻结源纹理不高于 158,466,352 B、RT 含深度不高于 48 MiB、关页引擎资源回到 0，
以及 §7 的像素限值。首屏/帧时使用相同设备、实际画布和画质的基线对照；移动端、冷启动和发布格式的
绝对预算仍需对应平台采样，不能由本桌面结果宣布达标。
`image-repeat.json` 的整图、图心和东侧边缘 ROI 均为零误差；后续布局变化仍须比较完整回归矩阵。

预览默认尊重网页选择；显式横竖测试结束会恢复原尺寸并重新挂载地图。
复用页面前先关闭旧地图，打开后检查页面根尺寸与可见区一致；生命周期和视口恢复使用宿主 API，
正常点选、缩放、小地图导航与画质操作使用鼠标输入。
过程中发生过外部标签关闭/跳转及进入后台的无效试跑，报告保留失败，未纳入上述性能基线。
最后的定点恢复检查见 `restore-smoke/viewport-report.json`：真实 UI 登录/设置入口通过，
宿主 API 连续关闭/重开两次通过，页面根始终等于 750×1475.19084 的实际可见区；
预览恢复网页选择的竖版 CSS 393×772.90625 / backing 393×773。

### O1 · 2026-09-24 实施记录

源码、派生产物、台账与本节同批提交；此前基线提交为 `be5d1043`。
证据根 `.cache/creator-preview/maporiginal-optimization/o1/`；`before/data` 与 `before/content` 保留 O0 输入快照。
素材审计输入指纹：`167c49c4c85ea747541853a1103e0ab4bb611eb15d0458b84b58fd5f6665a01c`。
重烘概览几何指纹：`ec44cd39bd9147ef8ae2fa625edb2944ff6789302bbde72099922aa0683b2c19`；
PNG SHA-256：`4c76b229d7c7629ac0c5ca198049aa6b0ff73c1dcb7bc15732620d5218fac033`。
新增公共 `texture_layout.py` 和生成契约 `atlas-layout.types.ts`，四类 reader 均通过 `textureId` 解析图片表。
每个逻辑条目的 ID、地貌变体、clientResId、prefab 变换与次序保留；没有裁边或进一步缩采样。
河岸和沙 top 的 PNG 逐字节未变；仅更新公共布局表达。

| 项目 | O0 | O1 实际结果 |
|---|---:|---:|
| 山体图片 / 页 | 26 个物理矩形 / 2048×4096 | 19 个 / 2048×2048，逻辑件仍为 26 |
| 道路图片 / 页 | 36 个物理矩形 / 1024² | 18 个 / 512×1024，逻辑条目仍为 36 |
| 城市图片 / 页 | 158 个 / 1024² | 158 个 / 512×1024 |
| 雪 top 图片 / 页 | 8 个 / 1024² | 8 个 / 512×1024 |
| 全部图集逻辑引用 / 物理矩形 / 独立图片 | 640 / 635 / 610 | 640 / 610 / 610 |
| 已加载源纹理 RGBA8 | 158,466,352 B（151.1253 MiB） | **135,397,680 B（129.1253 MiB）** |
| 18 张 PNG 文件大小 | 16,542,226 B | 16,049,603 B，减少 492,623 B |
| 运行时素材目录，排除 meta | 35 文件 / 20,124,431 B | 35 文件 / 19,638,469 B |
| 内容 TS | 16 文件 / 1,382,838 B | 17 文件 / 1,466,700 B，新增身份、版本和哈希元数据 |
| 运行时二进制 | 13 文件 / 3,494,841 B | 字节完全不变 |

源纹理实测减少 **23,068,672 B（22 MiB，约 14.56%）**；PNG 减少约 481 KiB，二者不能混算。
这里没有减少图片文件数，也没有将配置元数据增加包装成体积收益；TS 外置属于 O5。
地图关闭后源纹理与 RT 均为 0；CPU reader 仍持有 7,792,798 B，继续留在 O3 的释放范围。
RT 峰值仍为 48 MiB，L3 为 0。

核验结果：

- `generated-first.json / generated-repeat.json`：21 个 PNG、布局、类型/内容 TS 与二进制产物两次生成字节相同。
- `repack.json`：640 个逻辑引用逐片 RGBA、原画布及逻辑数据相同，15 个 `.bin/.bytes` 文件不变。
  城市保留 15 套件、1,642 个子件、249 个摆位，top 保留 1,899 条静态记录与完整动画节点。
- `fidelity.json`：原包 381 个入口、4,504 个展开节点与 635 次原切片对照通过，RGBA 不匹配为 0。
  道路 18 对基础/雪地原始切片及存储切片等价有生成期断言；运行时仍有 6,012 条雪地逻辑选件。
- `render-before / render-after`：同一 WebGL1 后端、1024² 输出、2× 超采样、动画时间 0，
  草地/雪/沙/洛阳/跨块范围五个场景的非 UV 几何指纹与批次逐字节一致。
  `render-comparison.json` 包含整图、每图 64 个 128² 局部区域及城墙/山/道路 ROI；
  最差局部 MAE 为 0.000306（8-bit），最大单通道差为 1，差值超过 8 的像素占比为 0。
- 概览与缩略图由已安装的新布局重烘；`overview-comparison.json` 的整图及各 64 个局部区域均通过，
  最差局部 MAE 为 0.000123（8-bit），最大差为 1。它们包含 UV 浮点插值的微小差别，不声称 PNG 字节相同。
- Python 装箱/审计/图片契约共 10 项通过；相关客户端 31 项、服务端内容 25 项、typecheck、安装器 `--check` 通过。

Creator 预览仍为 Chrome 153、Apple M4 / ANGLE Metal、WebGL2、DPR 1、目标 60 fps。
`portrait/report.json` 25/25、`landscape/report.json` 21/21 通过；地图自身各覆盖 20 步和 14 个有效采样窗口。
两次预览实测源纹理均为 135,397,680 B，八个 reader 的 Creator 编译源码与真源一致。

| 指标 | 竖版 | 横版回归 |
|---|---:|---:|
| 实际 CSS canvas | 393×772.90625 | 1624×702.65625 |
| 实际 backing canvas | 393×773 | 1624×703 |
| 第一帧近景地表 | 565.0 ms | 549.5 ms |
| 各窗口帧间隔 p95 最大值 | 35.3 ms | 35.6 ms |
| 各窗口每帧上传量 p95 最大值 | 760,252 B | 257,552 B |

横版的实际画布受 Creator 预览容器约束，与请求的 1624×750 分开记录，不能与 O0 的 1624×750 直接比较性能。
本次帧间隔 p95 高于 O0，关页窗口也出现约 33 ms 间隔，尚未确定其来源；**本批确认显存减量，不声称帧率提升**。
几何与固定时间画面的批次数不变，CPU/帧率的进一步优化仍按原计划处理，不通过删件获得数字改善。
横版测试结束按原尺寸重新挂载地图，恢复网页选好的竖版。最终 `restored-viewport.json`
实测 backing canvas 393×769，页面根与实际可见区均为 750×1467.55725；预览容器重排后的实际尺寸单独记录。

提交前全量校验：原工作区的 `verify:all` 被并行 3D 改动触发的
`scripts/verify-assets3d.test.mjs:115` 计数断言打断（预期 2、实际 5），与本轮地图图集无关。
另以 `be5d1043` 加本轮地图补丁建立独立检出，重新生成忽略的 UniFlex 预览资源并同步，
地图代码/素材与主工作区逐文件一致；`verify-all-isolated.log` 的 **`npm run verify:all` 退出码为 0**：
客户端 1,348、UniFlex 80、服务端 1,396 项全过，类型、资源、同步、清单与矩阵检查全过。
并行 3D 修改保留在原工作区，不纳入本批提交。静态素材、画面对照、预览与恢复的摘要见 `summary.json`。
